# Agent instructions

Read README.md, project1.py and images/nechelik_depth.png before planning. Explain the
contracts and wait for approval before editing. If instructions conflict, stop, show
the conflict and propose a minimal repair; wait for the student's explicit approval
before changing governing instructions. Do not silently ignore or repair them.

## Editing and validation
After approval, edit only Nechelik_Data.csv and project1.py. The provided Nechelik
class in project1.py is complete: implement extrapolate_depth and do not change the
class. Instruction changes need a reviewed proposal before edits. Protect all other
files, including README.md, submission-policy.json, images, environment, LICENSE,
.github and devcontainer files. Stop on unexpected changes. Run git diff --check. No
instruction changes during submission.
When the user says submit project 1, load .github/skills/submit-assignment/SKILL.md.
